/**
 * Each component receives the variables explained below
 *
 * @param {HTMLElement} el - Component's main DOM element
 * @param {Object} params - Params to be used
 */

// Mover conteudo parallax
// const $el           = $(el);
// const $boxTxt       = $el.find('.box-txt');

// $el.mousemove(function (e) {
//     parallaxIt(e, $boxTxt, -80);
//     // parallaxIt(e, "img", -30);
// });

// function parallaxIt(e, target, movement) {
//     var relX = e.pageX - $el.offset().left;
//     var relY = e.pageY - $el.offset().top;

//     TweenMax.to(target, 1, {
//         x: (relX - $el.width() / 2) / $el.width() * movement,
//         y: (relY - $el.height() / 2) / $el.height() * movement
//     });
// }
